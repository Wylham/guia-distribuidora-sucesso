/**
 * Default class for Abacate Pay exceptions and errors.
 *
 * It can be serialized to JSON through the `toJSON` method.
 */
declare class AbacatePayError extends Error {
    constructor(message: string);
    toJSON(): {
        name: string;
        message: string;
    };
}

export { AbacatePayError };
